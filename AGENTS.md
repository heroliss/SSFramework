# AGENTS.md — AI 项目协作入口

本项目以 Codex 为当前主要且已验证的编码 Agent。为让未来的其他 Agent 易于承接，跨工具真值只放在 `AGENTS.md`、`.agents/skills/`、文档、测试与项目工具中；不提前维护尚未采用产品的专用配置。本文件只放需要 Agent **始终记住、无法靠代码结构自然约束**的规则；不把某个客户端的按钮、快捷键或临时能力写成项目契约。场景化知识按目录、文档或 Skill 加载：

- 使用框架 API：`Assets/Game/AGENTS.md`
- 修改框架源码：`Assets/Game/Framework/AGENTS.md`
- 编写 Demo 章节：`Assets/Game/Framework/Demo/Scripts/Modules/AGENTS.md`
- 协作原理：`docs/ai-collaboration-guide.md`
- 其他 Agent 接入与 Handoff：`docs/ai-agent-onboarding.md`
- Unity MCP 项目要点：`docs/unity-mcp-tips.md`
- Project Skills 权威入口：`.agents/skills/`

只有实际采用某个新 Agent 时，才按它届时的官方能力增加最薄的发现或工具连接入口；不得复制规则正文，也不要求日常变更在多个客户端重复验证。根 `CLAUDE.md` 是当前保留的低成本导入入口，不代表 Claude 的 Skill、Unity 工具或交付闭环已经验证。

## 不可绕过的协作边界

### 1. 第三方库不直接修改

`Library/PackageCache/` 下的 YooAsset / UniTask / R3 等先用 Adapter、扩展或自定义 Interface 隔离。确需修改时先沟通，并记录补丁原因与升级影响；包更新会覆盖直接修改。

### 2. 场景与 Prefab 只经 Unity 编辑器修改

- `Assets/**/*.unity`、`*.prefab` **不得手改 YAML**；读取、搜索可用文件工具，写入必须用 Unity MCP 的 `unity_*` 工具。
- 动手前确认 Editor 不在 Play / 即将进入 Play；在 Play 就先停止。修改后调用 `unity_scene_save` 落盘。
- 经 MCP 启动 PlayMode 测试前，必须先执行菜单 `SSFramework/诊断/AI 自动化/PlayMode 测试预检（保存脏场景）`；它保存已有路径的脏场景并拒绝未命名场景，避免原生保存弹窗阻塞 MCP 队列。完整流程见 `docs/unity-mcp-tips.md`。
- 原因：直接写 YAML 容易破坏 GUID/fileID，绕过刷新/Undo，并制造难合并冲突。

### 3. 扩权、冲突与不可逆操作先沟通

以下情况停止并说明证据、选项与建议，不替用户拍板：

- 需求存在会显著改变结果的歧义，或技术选型尚未确定；
- 删除文件、推送代码、修改共享环境/权限等难恢复操作；
- 工具连续失败，或发现现有设计与请求冲突；
- 完成目标需要超出用户已授权范围的外部协调。

用户明确授予的自主权可覆盖“是否逐项确认”，但不扩大任务边界，也不取消上述安全检查。

### 4. AI 行为配置必须可追溯

- 新增或修改仓库内 `AGENTS.md`、`.agents/skills/` 或实际使用的产品接入层时，同步受影响的协作文档，并在提交正文记录“为什么、影响范围、如何验证”。
- 用户级配置是个人/机器状态，不是项目真值；除非用户明确要求，不为完成仓库任务修改它。
- `.agents/skills/<name>/SKILL.md` 是 Project Skill 权威正文；`agents/openai.yaml` 等产品元数据可选。其他客户端即使需要接入层，也只能路由到公共真值；不追求所有 AI 的配置对称。

## 版本控制信息

- 提交标题与正文默认使用简体中文；`feat(scope)` 等 Conventional Commits 前缀、代码标识符、命令、路径和原始错误文本保留英文。既有历史不为统一语言而重写。
- 非琐碎提交（跨多文件、公共 API、架构、行为契约，或实现与测试/文档联动）应在简洁标题后提供正文，说明**问题背景、核心设计与影响、验证结果**；必要时补充兼容性或迁移边界。
- 正文记录最终成立的事实和取舍，不机械罗列文件，不复述 diff，也不写已经失效的调试过程。
- typo、格式整理等单点琐碎变更可以只写一行；详细程度应与改动风险和审查成本相称。

## 代码与注释

### 中文可用性

- 面向使用者的菜单、窗口、按钮、提示、Console 说明、Demo 教学和主要文档默认使用简体中文；专业术语首次出现优先写“中文（English）”。Unity、asmdef、UPM、MCP、CLI、类型/成员名、命令参数、程序集名及第三方原始错误保留原文，避免翻译后无法检索。

### XML doc 泛型写法

`cref` 用 `{T}`，正文短引用用 `&lt;T&gt;`，多行 `<code>` 用 CDATA；正文裸 `<T>` 会破坏 XML，而 CDATA 内的 `<see>` 不可跳转。

### 注释写给维护者看

- 公共契约（类型与 Interface 成员）、受保护扩展点、关键生命周期入口，以及复杂时序、异步/取消/释放/缓存、反射与第三方 Adapter 边界，应说明职责、调用语义、所有权、失败行为和“为什么”。纯粹实现已文档契约且不增加语义的成员不重复正文，必要时用 `<inheritdoc/>`。
- 删除逐行翻译式注释；注释描述当前设计，不写改动历史或已删除方案。
- 第三方集成优先记录框架侧约束与踩坑边界。

## 协作姿态

把 AI 当有判断力的同事：发现用户判断可能有偏差、存在更高 Leverage 的实现或潜在风险时，按“观察 → 建议 → 理由”主动说明；最终决策权交给用户。先给可验证证据，不做无依据迎合。

规则本身也接受证据审查。除“不可绕过的协作边界”和机器锁定的确定性契约外，本仓库规则是当前条件下的默认决策，不是脱离上下文的机械禁令；“必须/不得”只用于会造成 bug、数据/权限风险或契约破坏的硬约束。局部事实与规则冲突时，先复核目标和风险：安全、可逆且不扩大授权的例外可以说明理由后采用，并同步修正规则；涉及硬边界、公共契约或外部副作用则先沟通。不得只为字面合规制造无价值改动，也不得静默绕过后让规则继续误导后人。

## 计划与委派

- 复杂、模糊、跨目录或高风险任务先形成可检查的工作计划，并在执行中更新；计划是任务产物，不要求用户切换客户端模式或按某个快捷键。只有缺少会显著改变结果的用户决策时才暂停。
- 明确的小改和同一迭代延续直接完成，不为流程本身增加审批。
- 默认由当前 Agent 完成。只有用户明确要求委派/并行，或已触发的 Skill 明确要求时才使用 Subagent；子任务还必须真正独立、不会争写同一文件，并在启动时说明读写边界与预期产出。
- 不把交互式开发机械拆成 plan → impl → review 流水线。未获委派授权时，公共 API、架构或非琐碎改动由当前 Agent 完成独立视角的 diff、边界和测试缺口自查。

## 持续改进

同类纠正反复出现、规则与代码冲突、固定检查只靠提醒、Skill 过时/重复，或常驻上下文逼近上限时，先说明观察与风险，再用 `propose-rule-evolution` 选择最窄载体；完整归类与放置表不重复常驻。截图流程用 `unity-screenshot`。客户端不能发现 Project Skill 时显式读取权威正文；缺少工具时缩小验证结论，不从产品目录猜规则。架构、目录或协作方案重大调整时同步 `docs/ai-collaboration-guide.md`。
