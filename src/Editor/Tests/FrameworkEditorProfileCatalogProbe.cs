using UnityEngine;

namespace Game.Framework.Editor.Tests
{
    /// <summary>Profile Catalog 的资产发现探针；文件名与类型名一致，确保 Unity 建立稳定的 MonoScript 身份。</summary>
    internal sealed class FrameworkEditorProfileCatalogProbe : ScriptableObject { }
}
